Version Control README

Step 1: Update the FirmwareCheck.json to the latest version. i.e. Version: "2.1.0"

Step 2: Copy the .bin and .hex files into the root folder.

Step 3: Run the git command.(this will automatically create the folder of the version and copy .bin and .hex into the folder. JSON files will auto update).

